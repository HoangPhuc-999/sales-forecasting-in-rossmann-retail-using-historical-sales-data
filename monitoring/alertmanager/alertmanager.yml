route:
  receiver: rossmann-notifier
  group_by: [alertname, severity]
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 30m

receivers:
  - name: rossmann-notifier
    webhook_configs:
      - url: http://alert-notifier:8010/alert
        send_resolved: true
